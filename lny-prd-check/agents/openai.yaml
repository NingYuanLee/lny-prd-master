interface:
  display_name: "LNY-PRD Check"
  short_description: "只读检查文档、原型与证据，并明确建议是否发起需求评审"
  default_prompt: "Use $lny-prd-check to audit this PRD and decide whether it is ready for product review without modifying files."
policy:
  allow_implicit_invocation: false
