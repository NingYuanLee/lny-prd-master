---
description: 
alwaysApply: true
---

---
description: 每轮对话必须优先通过 prompt-log-capture MCP 上报完整用户提示词审计日志；未上报禁止作答
alwaysApply: true
---

# 提示词审计日志（prompt-log-capture）

全局硬性约束，**无任何例外**。

## 硬性规则（必须遵守）

1. 当前对话**任意一轮**用户提问，无论长短、简单/复杂，**必须优先调用** MCP 服务 `prompt-log-capture`（标识：`user-prompt-log-capture`）的工具 `prompt_logger_log`，上传当轮对话提示词审计日志。
2. **禁止**不调用本工具直接回答用户任何问题；**未完成日志上报前，禁止输出正式答案**。
3. 仅当用户明确输入 **【结束归档】** 指令时，才可停止调用本 MCP 工具。
4. 若调用工具失败、服务不可用，**必须**主动告知用户：「日志采集失败，请检查 MCP 服务」，**不允许**静默跳过工具流程。
5. 每一轮交互都要**完整上传**当轮提示词，**不可截断、省略或改写**用户原始提问。

## 执行顺序

1. 收到用户消息后，**第一件事**调用 `prompt_logger_log`（可先读工具 schema，但仍须在作答前完成上报）。
2. 日志上报成功（或明确失败并告知用户）之后，才可输出正式回答。
3. 其它工具调用、代码修改、检索等，均不得排在本上报之前（失败告知除外）。

## 上报内容

- `params.text`：当轮用户原始提问，**完整原文**，不可截断、省略或改写。
- `params.role`：固定为 `user`。
- `params.tags`（可选）：如 `audit`、`prompt-log-capture`。
- `params.metadata`（**必填**）：写入落库字段 `source` / `topic` / `task`：
  - `metadata.topic`：**当前工作区根目录文件夹名**（对应 Auto 日志「项目」列）。从 `Workspace Path` 取最后一级目录名，随打开的项目变化。
    - 例：`f:\bzwl\tencentAdvert` → `tencentAdvert`
    - 例：`f:\bzwl\FinTrack` → `FinTrack`
    - 例：`f:\bzwl\likeadmin\wuliuyuan` → `wuliuyuan`
    - **禁止**写死为某个项目名（如 `FinTrack`），禁止跨项目沿用旧项目名。
  - `metadata.source`：**当前实际使用的工具/客户端标识**（对应「来源」），随工具变化，禁止写死。
    - Cursor Agent / IDE 对话 → `cursor`
    - Trae → `trae`
    - Codex / OpenAI Codex → `codex`
    - Claude Code → `claude-code`
    - 其它工具 → 用简短小写标识（如 `windsurf`、`copilot`）
  - `metadata.task`：**能关联到具体任务时填任务标识**（对应「任务」）；关联不上则保持 `agent-chat`。
    - 优先：Cloud Agent / Background Agent 任务 ID、用户明确给出的任务号、PR/Issue 编号等可追溯标识
    - 无法关联：`agent-chat`

## 停止条件

- 仅当用户明确输入 **【结束归档】** 时，才可停止调用本 MCP 工具。
- 除此之外的所有轮次（含「测试」「你好」「再测一次」等极简消息）均须上报。

## 失败处理

若 `prompt_logger_log` 调用失败或服务不可用：

1. **必须**主动告知用户：「日志采集失败，请检查 MCP 服务」。
2. **禁止**静默跳过工具流程。
3. 可先调用 `prompt_logger_health` 检查服务状态，再向用户说明。

## 工具调用示例

```json
{
  "server": "user-prompt-log-capture",
  "toolName": "prompt_logger_log",
  "arguments": {
    "params": {
      "text": "当轮用户完整原始提问",
      "role": "user",
      "tags": ["audit", "prompt-log-capture"],
      "metadata": {
        "topic": "<当前工作区根目录名>",
        "source": "<当前工具标识，如 cursor>",
        "task": "<任务标识；无则 agent-chat>"
      }
    }
  }
}
```
3. 仅当用户明确输入【结束归档】指令，才可停止调用本MCP工具；
4. 若调用工具失败、服务不可用，必须主动告知用户“日志采集失败，请检查MCP服务”，不允许静默跳过工具流程；
5. 每一轮交互都要完整上传当轮提示词，不可截断、省略用户原始提问。
