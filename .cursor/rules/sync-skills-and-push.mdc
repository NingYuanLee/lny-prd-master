---
description: 技能包落盘后同步 Cursor skills 并推送到 GitHub
alwaysApply: true
---

# 落盘后同步与推送

本仓库是 **LNY-PRD 技能包**（不是业务 PRD 项目）。凡本轮已把改动**写入磁盘**（`lny-prd-*/`、`.cursor/rules/`、根 `README.md` 等技能相关文件），在回合结束、向用户汇报之前必须做完下面两步。

## 1. 同步 Cursor skills

将仓库内各 `lny-prd-*/` **镜像**到用户 Cursor 技能目录（Windows 默认 `C:\Users\<user>\.cursor\skills\`；其它系统为 `~/.cursor/skills/`）：

```powershell
$src = "<本仓库根>"
$dst = "$env:USERPROFILE\.cursor\skills"
Get-ChildItem $src -Directory -Filter "lny-prd-*" | ForEach-Object {
  robocopy $_.FullName (Join-Path $dst $_.Name) /MIR /XD __pycache__ .git /NFL /NDL /NJH /NJS /nc /ns /np
}
```

- `robocopy` 退出码 `0–7` 视为成功；`≥8` 必须修复后重试。
- 只同步 `lny-prd-*`，不要把 `examples/` 整包拷进 skills。

## 2. 提交并推送 GitHub

对本仓库执行（需 `git_write` + `network`）：

1. `git status` / `git diff` / `git log -5 --oneline` 确认变更与提交风格。
2. 暂存本轮技能相关改动并 `git commit`（信息用 HEREDOC；说明「为什么」；遵循已有 `feat:` / `fix:` 风格）。
3. `git push origin HEAD`（当前跟踪分支，一般为 `main`）。
4. **禁止** `--force` 推 `main`；**禁止**改 git config；钩子失败则修完再**新开**提交，不 amend 已推送提交。

无新变更可跳过 commit/push，但仍须确认 skills 目录与仓库一致（若本轮改过 `lny-prd-*`）。

## 例外

- 用户明确说「不要推送 / 不要同步」时，跳过对应步骤并在回复里说明。
- 仅只读问答、未落盘文件时，不做同步与推送。
