# PAGE-MP-009 时间轴 页面详述

## 1. 基本信息

| 属性 | 内容 |
|------|------|
| 页面编号 | PAGE-MP-009 |
| 页面名称 | 时间轴 |
| 所属终端 | 微信小程序 |
| 所属模块 | 套件 |
| 小程序路由 | `pages/kit/timeline` |
| 前端路由 | 无 |
| 操控形态 | 移动端 |
| 骨架码（桌面端必填） | 无 |
| D5弹窗数（桌面端必填） | 无 |
| 交互体验 | 标准 |
| 视觉细节 | 标准 |
| MasterGo | 无 |

## 2. 页面层级及布局描述

### 2.1 移动端结构（操控形态=移动端时填写）

#### │ L0 系统层
- 顶栏模式 **标准**（状态栏背景不透明）；真机预览状态栏固定顶、不随页滚，电量/信号靠右；页内可点控件避让右上胶囊，禁止在胶囊区放按钮；圆角/曲面屏左右下及四角留安全距；无 TabBar

#### │ L1 固定导航层
- 无 TabBar；页内顶栏左侧返回、标题「时间轴」，右侧避让胶囊
- 无贴底主操作条

#### │ L2 主体滚动层
- 整页滚动时间轴节点

#### │ L3 模块容器层
- `md-timeline`：左侧竖状节点+连线（`__rail` `__node` `__line`），右侧**横卡文本**（`md-card--row` 无左图；标题 + 说明 + 时间；正文内可选 `__photos` 多小图）
- 节点含已完成 / 当前 / 待办（「已提交」「审核中」「待发布」「已完成」）；点节点切换当前高亮

#### │ L4 内容元素层
- 可点元素无下划线，按下有反馈，热区 ≥44px；触屏不靠 hover

#### │ L5 浮层与反馈层（如有）
- 无

### 2.3 交互体验设计（必填）

- **观感与舒适**：标准顶栏标明「时间轴」；左轨右横卡文本对齐节点，当前节点外圈提示。
- **加载 / 空 / 失败**：本页为套件样例、进页即展示节点，无列表请求；无骨架。
- **键盘与焦点**：触屏不靠 hover。
- **首次进入**：无首次引导；打开即见四条节点。
- **动效与过渡**：点节点切换高亮（快）。
- **微反馈**：节点卡片按下有反馈。
- **收纳与浮层**：主路径整页直出，不进半屏。

## 4. 本页复用组件（如有）

无

## 5. 页面拆分说明（可选）

- **是否由更大页面拆分而来**：否

**关联页面索引**：

| 页面编号 | 页面名称 | 小程序路由 | 关系说明 |
|----------|----------|------------|----------|
| PAGE-MP-001 | 首页 | `pages/index/index` | 返回 |

## 6. 关联接口

无（套件样例页）

## 7. 备注

套件样例。桌面对应 PAGE-AD-007。
