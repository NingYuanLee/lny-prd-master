interface:
  display_name: "LNY-PRD Review"
  short_description: "按版本证据评审范围，检查同页Feature原子性，并在确认后记录决议"
  default_prompt: "Use $lny-prd-review to evaluate the target version evidence, enforce PAGE scope atomicity, and propose a concrete delivery conclusion for product confirmation or adjustment."
policy:
  allow_implicit_invocation: false
