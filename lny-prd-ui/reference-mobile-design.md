# 移动端设计词典（② 主责 · 与套件分工）

写 `ui/PAGE-*.md` / `ui/COMP-*.md` 前扫一眼。目标：像完整移动端设计规范一样覆盖原则、视觉、组件、布局、交互，但**不把规格写成视觉稿参数表**。

皮肤与具体 px/色值由 **⑥ `lny-prd-prototype` 套件 + `gold/`** 落地。用户另给品牌规范时：只覆盖冲突项，其余仍用本词典 + §1.7.0。

## 分工（防误读）

| 板块 | ② 规格写什么 | ⑥ / 套件写什么 | 规格里不要写 |
|------|--------------|----------------|--------------|
| 原则与目标 | 本页主事、清晰/一致/可反馈怎么落地 | 金样结构 | — |
| 视觉 | 主次层级、图文比例、疏密节奏（语义） | 色板、字号、圆角、间距 token | `#hex`、`14px`、字号表、阴影参数表 |
| 组件 | 用哪类控件、状态、收纳 | `md-*` 类名与行为 | 自造组件名当皮肤 |
| 布局与适配 | L0/L1/L2、贴顶贴底、安全区、弹性分区 | 375 预览、safe-area、flex 金样 | 自拟 750 稿尺寸当唯一真理 |
| 交互与反馈 | 手势结果、按下/加载/成功/失败、空失败 | Toast/Dialog/骨架实现 | ms、hover 当唯一态 |
| 文档元信息 | `ui_manifest` 索引 + PAGE 表头 | 工具包版本见仓库 README | — |

**正确理解**：「规格不写色值清单」≠「不用设计视觉」。② 必须写清**层级与节奏**；⑥ 必须按金样把视觉做满。

## 1. 设计原则与目标

| 原则 | 含义 | ② 落笔 |
|------|------|--------|
| 清晰 Clarity | 一屏一眼看懂主事 | 「首屏只打 {一件}」 |
| 一致 Consistency | 同类页同结构、同反馈 | 对夹具页型；勿混顶栏形态 |
| 反馈 Feedback | 操作必有可见结果 | 按下 / Toast / 确认 / 骨架 |
| 效率 | 少跳转、热区够大 | 筛选半屏；热区 ≥44px 语义 |

设计目标（写进本页或项目级）：统一视觉语言、降低误触、主路径不进弹窗。

## 2. 视觉规范（语义层）

② 用下表描述观感；禁止抄成 CSS 参数表。

| 维度 | ② 怎么写 | ⑥ 默认（套件） |
|------|----------|----------------|
| 颜色 | 「主操作强调；错误价/危险红；次要灰字」 | `--md-primary` / `--md-error` / `--md-text-secondary` 等 |
| 字体 | 「标题强于副文；元信息最小可读」 | `.md-h*` / `__title` / `__subtitle` / `__meta` |
| 图标 | 「与文案同组；元信息用小图标」 | `data-icon`；闭集见 `reference-icons` |
| 间距圆角 | 「区块有呼吸；卡片勿厚重浮块」 | `--md-space: 8px` 倍数；触屏卡 12px 圆角 |
| 图片 | 「列表封面 1:1；详情主图 16:9」 | `md-media-ph--n` / `md-swiper--wide` |

列表横卡文字层级（有字段才写）：标题 → 副标题 → 摘要截断 → 底栏元信息（时间/浏览/点赞）+ 价格。详见 prototype `reference-kit`「触屏列表卡片排版」。

## 3. 组件规范（工具箱）

规格点名形态；⑥ 只用套件，禁止裸 HTML。

| 类别 | 规格用语 | ⑥ 类名（摘要） |
|------|----------|----------------|
| 基础 | 按钮主/次/线框、输入、单元格/横卡 | `md-btn` `md-field` `md-card--row` |
| 表单 | 开关、单选多选、下拉、轮盘 | `md-switch` `md-check` `md-radio` `md-select` `data-wheel` |
| 反馈 | 对话框、Toast、骨架、空态 | `md-dialog` `snackbar` `md-skeleton` `md-empty` |
| 展示 | 标签、头像/缩略、轮播、进步/进度 | `md-chip` `md-card__thumb` `md-swiper` `md-advance` `md-progress` |
| 导航 | TabBar、返回顶栏、宫格 | `md-tabbar` `md-appbar` `md-king` |

触屏搜索框：**仅左图标 + 输入**，无「搜索」文案、无右侧搜索按钮。

热区：可点控件语义 ≥44px；触屏不靠 hover 当唯一态。

## 4. 页面布局与适配

| 项 | ② / L 层级 | ⑥ |
|----|------------|---|
| 结构 | 状态栏模式 + 顶栏/无顶栏 + 内容 + Tab/操作条 | `md-immersive` / `md-standard`；金样四种顶栏 |
| 安全区 | 左右下+四角；MP 胶囊避让 | `--md-safe-*`；MP 顶栏/状态栏避让 |
| 弹性布局 | 「左图右文可伸缩；主文与底栏分开」 | `__main` + `__foot` flex |
| 适配基准 | 不写死唯一稿宽 | 预览逻辑宽 **375**；`viewport-fit=cover` |

顶栏四选一（禁止混用）：① 16:9+slogan ② 16:9+返回标题 ③ 标准返回标题 ④ 两倍高度封面。

## 5. 交互与反馈

| 项 | ② | ⑥ |
|----|----|----|
| 手势 | 点按、滑动列表、半屏拖关闭（语义） | 套件抽屉/列表滚动 |
| 按下 | 「按下有缩放/变暗」 | 套件 `:active` |
| 加载/空/失败 | 舒适默认六项 | 骨架 / 插画空态 / 可重试 |
| 成功/轻结果 | Toast | 触屏居中半透明黑底 |
| 破坏性 | 确认框 | `ProtoPage.confirm` |
| 动效 | 快/中/慢、滑入/淡入 | 套件默认时长；禁止闪白 |

## 6. 文档基础信息

| 项 | 落点 |
|----|------|
| 版本 | 项目 `versions/{v}/`；工具包版本见仓库根 `README.md` |
| 适用范围 | `main_spec` 第4章终端；本词典默认覆盖 MP/H5/APP 触屏；桌面另遵 §1.4.3 |
| 修订 | 改根规范 → `iteration_notes`；改技能包 → 工具包版本号 |

## 写 §2.3 时的最小清单

1. 原则：本页主事 + 清晰/反馈一句  
2. 视觉：主次与图文比例（语义，无色值表）  
3. 组件：关键控件与状态  
4. 布局：顶栏形态、固定区、安全区  
5. 交互：加载/空/失败、按下、成功/失败分流  
6. 舒适默认六项（或「无」并说明）

未完成自检：删掉 §2.3 后 ⑥ 只能画线框 → 本页未完成。
