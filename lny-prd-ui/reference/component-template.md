# ui/COMP-*.md 模板
```markdown
# {组件名称} 组件详述

## 1. 基本信息

| 属性 | 内容 |
|------|------|
| 组件名称 | {组件名称} |
| 组件编号 | {COMP-001} |
| 设计前提 | {多页复用 / 多态展示 / 两者兼有} |
| 独立视觉状态机 | {是 / 否；否时通常并回 PAGE 分区} |
| 适用终端 | {MP / APP / H5 / PC / AD，可多选} |
| 覆盖页面 | {PAGE-*.md 列表；仅多态暂未复用时可为单页} |
| 来源 | {新建 / 沉淀复用 / 既有组件升级} |

## 2. 用途与范围

- **用途**：{一句话说明解决的问题}
- **判定依据**：{满足多页复用 / 多态展示 / 两者兼有的简要说明}
- **适用场景**：{在哪些页面分区复用}
- **边界与排除项**：{哪些场景不适用}
- **单职责**：本组件自洽管理全部 UI 状态；若由多块视觉分区组成，说明 **为何同属一个 COMP**（同一状态机 + 视觉相邻）

## 3. 结构与交互

- **结构分层**：{容器、内容区、操作区等}
- **交互表现**：{上游既定操作触发后，组件如何展开、折叠、半屏或弹窗；不得补设业务条件}
- **动效与微反馈**：{按下/悬停/焦点；开闭过渡；成功/失败反馈；无则写 **无** 并说明为何瞬切可接受}
- **状态机与区块联动（如有）**：{状态枚举、转移条件、相邻子区如何随状态联动；无则写 **无**}
- **状态切换入口**：{页内 TabBar/Tabs/SegmentedControl/按钮组/无（单态）；仅产品页内语义，禁止写预览壳状态演示、postMessage 等；无页内入口时由 `/lny-prd-prototype` 状态演示推断}

## 4. UI 状态矩阵（必填）

| 状态 | 触发条件 | 展示要点 | 可执行操作 |
|------|----------|----------|------------|
| {仅填写本组件实际可触发的状态；无此状态必须删行} | {真实触发条件} | {必须能在页面中辨认的视觉差异} | {真实可执行操作；无则写无} |

状态矩阵只定义可见 UI 差异，不定义业务状态机。不要整段保留 `loading / empty / error / edit / default`；至少保留真实默认态。`loading`、`empty`、`error` 只在上游规格已有对应情形时填写；`edit`、`disabled`、`pending` 等只描述既定状态的视觉实现，触发条件引用上游事实而不在此重定义。状态演示壳层会按本表逐字生成，遗漏或泛化都会被原型验收拦截。

- **默认态 / 交互态**：{按下 / 焦点 / disabled / 悬停（桌面增强）；须能脱离 hover 辨认；无则写 **无**}

## 5. 关联页面索引

> 页面名称与路由查 `ui_manifest` §3.2，本表不重复。

| 页面编号 | 使用位置 | 备注 |
|----------|----------|------|
| {PAGE-MP-001} | {如：L2 顶部} | {可选} |
| {PAGE-AD-001} | {如：D1-1 搜索栏} | {可选} |

## 6. 备注

{仅补充组件视觉与交互表现；不得写字段、接口、权限、合规、审核或产品策略。无则写 **无**}
```
