# ui/PAGE-*.md 模板
**填写约束**：`交互体验` 取值 **`简单` / `标准` / `较复杂` / `极复杂`**；`视觉细节` 取值 **`粗糙` / `标准` / `精致` / `极精致`**（均只在本 PAGE 明细维护，**仅供 ⑨ 估点**；⑥ 出原型**忽略此档**、一律按金样 + 设计词典高保真，**不得因「粗糙」少画层级或控件**）。**§2.3 交互体验设计必填**（含视觉主次与反馈，但不要写 `#hex`/阴影参数表）。旧值交互 `低`/`中`/`高` → `简单`/`标准`/`较复杂`；旧值视觉 `低`/`中`/`高` → `粗糙`/`标准`/`精致`。

```markdown
# {PAGE-终端-序号} {页面名称} 页面详述

## 1. 基本信息

| 属性 | 内容 |
|------|------|
| 页面编号 | {PAGE-MP-001} |
| 操控形态 | {移动端 / 桌面端} |
| 骨架码（桌面端必填） | {D0 / D1-1 / D1-2 / D1-3；移动端填 **无**} |
| D5弹窗数（桌面端必填） | {非负整数；只计弹窗，不计 Toast；无则 **0**；须与 §2.2 D5 弹窗清单表行数一致} |
| 交互体验 | {简单 / 标准 / 较复杂 / 极复杂} |
| 视觉细节 | {粗糙 / 标准 / 精致 / 极精致} |
| MasterGo | {链接或无} |

> 页面名称、所属终端、页面地址、MP 包模块/主包分包、PC/AD 菜单分组与生命周期以 `ui_manifest.md` §3.2 为唯一事实源，本页不重复。菜单分组不是 Feature `MODULE-*`。

## 2. 页面层级及布局描述

> 先写结构（L0～L5 或 D0/D1/D5），再写 **§2.3 交互体验设计**。§2.3 不可省。局部组件状态见 `ui/COMP-*.md`，本页不重写矩阵。

### 2.1 移动端结构（操控形态=移动端时填写）

#### │ L0 系统层
- {顶栏模式：**沉浸式**（状态栏透明）或 **标准**（状态栏不透明）；真机预览状态栏固定顶、电量/信号靠右；标准顶栏左右贴边；沉浸式/封面顶栏上的可点控件避让右上胶囊；正文左右下及四角留安全距；壳层 TabBar 如有}

#### │ L1 固定导航层
- {有 TabBar：写「无页内顶栏，TabBar 贴底固定」；列表页另写搜索行+筛选行贴顶固定。无 TabBar 的二级页才写返回顶栏。展示次要操作 / 表单提交写贴底主操作条}

#### │ 步骤区（有序向导 / 引导分步页 **必填**，见 `PT-STATE-FLOW`；其它页写 **无**）
- **导览形态（须逐字点名其一）**：{数字步骤 | 分段进步 | 无极进度}（桌面 AD：**数字步骤 | 分段进步** 二选一）
- **共 {N} 步**；所选一种导览须**一眼看懂**当前第几步 / 共几步
- **禁止**「步骤条+进步条」「分段+无极」等**组合措辞**；L3 **只写所选一种**对应控件（`md-stepper` **或** `md-advance` **或** `md-progress--lg`）

#### │ L2 主体内容层
- **【下沉首屏】**（可选）：{背景 / slogan / Banner / Hero；钉底不滚；须 L0 沉浸式 + 下沉滚过语义。无则写「无下沉首屏」}
- **【滚动容器】**（必有）：{`body` > **`sheet`** > 分区顺序；贴边写 `--flush-x`；**禁止** body 直放 module}

#### │ L3 模块容器层
- {每个模块：普通（随滚）或 **吸顶**（写吸顶对象与顶距）。卡片、分区、列表、表单块；随滚 Banner 写在此，勿与 L2 下沉混用}

#### │ L4 内容元素层
- {标题、文案、按钮、图片、输入、标签}

#### │ L5 浮层与反馈层（如有）
- {弹窗、抽屉、Toast、遮罩等页面级浮层；禁止写子组件内部状态切换详述}

### 2.2 桌面端结构（操控形态=桌面端时填写）

> 骨架码仅能填写：**D0 / D1-1 / D1-2 / D1-3**；**禁止写 D1**。

#### │ D0 空白布局（如使用）
- {全页结构要点}

#### │ D1-1 标准内容区（如使用）

##### ││ 页内签（如有）
- {写在搜索栏上方；点签整块切换搜索栏+功能栏+列表+汇总分页。无则写 **无页内签**}

##### ││ 搜索栏
- {搜索/筛选控件的排列、收纳、焦点与反馈；字段、选项、默认值和参数映射只读上游 API/Feature，不在此定义}

##### ││ 功能栏
- {既定操作的按钮形态、主次、排列与反馈；权限和业务触发条件只读上游 Feature，**不要**把页内签放在本栏}

##### ││ 列表区（含操作列）
- {展示列与操作列；操作列右侧冻结不横滚；有勾选列则左侧冻结不横滚；冻结列内侧浮层阴影}

##### ││ 汇总栏 + 分页栏
- {汇总逻辑、分页参数；默认贴内容区底部固定，横向滚动条贴表底；例外须写明}

#### │ D1-2 表单内容区（如使用）
- {既定字段的视觉分组、展示态 / 录入态 / 编辑态与保存反馈；字段契约、校验和提交规则不在此定义}

#### │ D1-3 自定义内容区（如使用）
- {非标布局分区与交互规则}

#### │ D5 浮层与反馈层（如有）

> **弹窗清单（估点用）**：只列 **弹窗**（表单/列表/展示）；Toast、抽屉若非弹窗形态可写在下方说明但 **不计入** `D5弹窗数`。无弹窗时表内一行写 **无**，且基本信息 `D5弹窗数=0`。

| 弹窗名称 | 类型（表单/列表/展示） | 触发入口 |
|----------|------------------------|----------|
| {例：新建表单弹窗} | 表单 | 功能栏「新建」 |
| {若无} | 无 | 无 |

- {其它浮层说明：Toast、抽屉、错误提示等；禁止写子组件内部状态切换详述}

### 2.3 交互体验设计（必填，移动/桌面都写）

> 按 [`visual-rules.md`](visual-rules.md) **§1.7**（未另给规范则用 **§1.7.0**）。写语义不写 CSS。删掉本节后⑥只会画出线框 → 未完成。

- **观感与舒适**：{首屏主次；一屏几件事；固定区 vs 滚动；图文比例。无例外则写「按默认体验规范」}
- **向导进度（有序向导 / 引导分步页必填）**：所选一种导览须**一眼看懂共 {N} 步**；仅数字步骤可点跳步；分段/无极只展示、靠上一步/下一步；禁止写多种导览并列
- **加载 / 空 / 失败**：{骨架；空态插图与引导；失败可重试。无则写无并说明}
- **键盘与焦点**：{桌面 Tab/回车/焦点可见；触屏不靠 hover。无则写无}
- **首次进入**：{没数据/没权限时第一眼。无则写无首次引导}
- **动效与过渡**：{必须过渡的开闭/轮播/签切换；允许瞬切的；用快/中/慢或「过渡滑入」}
- **复杂交互效果（有则逐项）**：{除一次直接点击外，逐项写“触发 → 可见过程/过渡 → 结果状态 → 取消/失败/禁用（适用时）”；归属 COMP 则引用对应状态矩阵}
- **微反馈**：{按下；提交中；成功；失败}
- **收纳与浮层**：{默认露出什么；折叠/半屏方向/弹窗/确认/Toast 各管什么；为何不新开页}

## 3. 本页可见操作清单（可选，D1-1 推荐填写）

> 只登记上游已确定操作的 UI 表现。不得在此新增操作、权限、状态限制、跳转目标、接口或提交规则。

| 操作名称 | 位置 | 控件形态与主次 | 可见反馈 |
|----------|------|----------------|----------|
| {例：导出} | {功能栏} | {次要图标按钮} | {处理中 / 成功 Toast / 失败 Alert} |
| {例：编辑} | {操作列} | {常规图标按钮} | {打开既定表单弹窗的视觉过渡} |

## 4. 本页复用组件（如有）

> 此处只索引；各组件状态见 `ui/COMP-*.md`，不在本页重写。

| 组件名称 | 使用位置 | 详述链接 |
|----------|----------|----------|
| {组件名称} | {如：L2 顶部} | {ui/COMP-001.md} |

## 5. 页面拆分说明（可选）

- **是否由更大页面拆分而来**：{是/否}
- **拆分原因**：{仅写视觉复杂度与独立 UI 状态机，例如原页含 3+ 独立状态机而拆页；用户路径与业务流程归 Feature}

## 6. 备注

{仅补充本页视觉与交互表现；不得写用户路径、画像策略、权限合规、业务规则或接口字段。无则写 **无**}
```
