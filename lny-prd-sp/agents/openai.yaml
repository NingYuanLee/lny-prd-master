interface:
  display_name: "LNY-PRD Story Points"
  short_description: "按指定版本汇总前后端故事点、信号缺口并生成估点报告"
  default_prompt: "Use $lny-prd-sp to calculate FE and BE story points for the requested PRD version."
policy:
  allow_implicit_invocation: false
